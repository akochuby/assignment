# Simple Rest API using spring+hibernate
To build the project on Windows:
```gradlew.bat war```

To build the project on Linux:
```./gradlew war```

Produced war file will be located in
```./build/libs/assignment.war```
